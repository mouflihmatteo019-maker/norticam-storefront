import { Star } from 'lucide-react';
export function ReviewStars({ value, size=16, label=true }: { value:number; size?:number; label?:boolean }) { const rounded=Math.round(value);return <span className="inline-flex items-center gap-1" aria-label={`${value.toFixed(1)} sur 5`}><span className="inline-flex text-amber-400" aria-hidden="true">{[1,2,3,4,5].map(star=><Star key={star} size={size} fill={star<=rounded?'currentColor':'none'} className={star<=rounded?'':'text-slate-300'} />)}</span>{label&&<strong className="ml-1 text-sm text-slate-950">{value.toFixed(1).replace('.',',')}</strong>}</span>; }
