/** Design reference: NORTICAM storefront — exact shell hierarchy, sticky nav and cart drawer. */
import { BrandMark } from "@/components/BrandMark";
import { useCart } from "@/contexts/CartContext";
import { euro } from "@/lib/store-data";
import { Menu, Minus, Plus, ShieldCheck, ShoppingBag, Trash2, X } from "lucide-react";
import { useEffect, useState, type ReactNode } from "react";
import { Link, useLocation } from "wouter";

const navItems = [{ href: "/boutique", label: "Boutique" }, { href: "/comparatif", label: "Comparatif" }, { href: "/quiz", label: "Trouver ma dashcam" }, { href: "/conseils", label: "Conseils" }];

function CartDrawer() {
  const { lines, isOpen, close, itemCount, total, checkoutUrl, setQuantity, remove } = useCart();
  useEffect(() => { const onKey = (event: KeyboardEvent) => event.key === "Escape" && close(); window.addEventListener("keydown", onKey); return () => window.removeEventListener("keydown", onKey); }, [close]);
  if (!isOpen) return null;
  return <div className="fixed inset-0 z-[70]" role="dialog" aria-modal="true" aria-label="Votre panier"><button className="absolute inset-0 bg-slate-950/35 backdrop-blur-[2px]" aria-label="Fermer le panier" onClick={close} /><aside className="cart-drawer absolute bottom-0 right-0 flex h-[min(86vh,720px)] w-full max-w-md flex-col rounded-t-[2rem] bg-white p-5 shadow-2xl sm:bottom-4 sm:right-4 sm:h-[calc(100vh-2rem)] sm:rounded-[2rem] sm:p-6"><div className="flex items-center justify-between border-b border-slate-100 pb-5"><div><p className="text-[10px] font-bold uppercase tracking-[0.16em] text-[#1672d8]">Votre sélection</p><h2 className="font-display text-2xl font-extrabold tracking-tight text-slate-950">Panier {itemCount ? `(${itemCount})` : ""}</h2></div><button type="button" onClick={close} className="grid h-10 w-10 place-items-center rounded-full bg-slate-100 text-slate-800 hover:bg-slate-200" aria-label="Fermer"><X size={19} /></button></div>{lines.length ? <><div className="min-h-0 flex-1 space-y-4 overflow-y-auto py-5">{lines.map(({ product, variantId, variantTitle, unitPrice, quantity }) => <div className="flex gap-3" key={variantId}><div className="grid h-20 w-20 shrink-0 place-items-center overflow-hidden rounded-2xl bg-slate-100"><ShieldCheck className="text-[#1672d8]" /></div><div className="min-w-0 flex-1"><div className="flex justify-between gap-2"><h3 className="font-display text-sm font-bold text-slate-950">{product.shortTitle}</h3><span className="text-sm font-bold text-slate-950">{euro(unitPrice * quantity)}</span></div><p className="mt-0.5 text-xs text-slate-500">{variantTitle === "Default Title" ? product.vendor : variantTitle}</p><div className="mt-3 flex items-center justify-between"><div className="flex items-center rounded-full border border-slate-200 p-0.5"><button onClick={() => setQuantity(variantId, quantity - 1)} className="grid h-7 w-7 place-items-center rounded-full hover:bg-slate-100" aria-label="Réduire"><Minus size={13} /></button><span className="w-7 text-center text-xs font-bold">{quantity}</span><button onClick={() => setQuantity(variantId, quantity + 1)} className="grid h-7 w-7 place-items-center rounded-full hover:bg-slate-100" aria-label="Augmenter"><Plus size={13} /></button></div><button onClick={() => remove(variantId)} className="p-2 text-slate-400 hover:text-red-600" aria-label={`Retirer ${product.shortTitle}`}><Trash2 size={16} /></button></div></div></div>)}</div><div className="border-t border-slate-100 pt-5"><div className="mb-4 flex items-end justify-between"><span className="text-sm text-slate-500">Total</span><span className="font-display text-2xl font-extrabold text-slate-950">{euro(total)}</span></div><button onClick={() => window.location.assign(checkoutUrl)} className="flex h-13 w-full items-center justify-center rounded-full bg-[#1672d8] px-5 text-sm font-bold text-white shadow-[0_10px_24px_rgba(22,114,216,0.25)] transition hover:bg-[#075dbb] active:scale-[.98]">Continuer vers le paiement Shopify</button><p className="mt-3 text-center text-xs leading-5 text-slate-500">Le paiement final n’est pas intégré ici : vous serez redirigé vers le panier et le checkout sécurisés de NORTICAM sur Shopify.</p></div></> : <div className="grid flex-1 place-items-center text-center"><div><div className="mx-auto grid h-16 w-16 place-items-center rounded-full bg-blue-50 text-[#1672d8]"><ShoppingBag size={25} /></div><h3 className="mt-4 font-display text-xl font-extrabold text-slate-950">Votre panier est vide</h3><p className="mx-auto mt-2 max-w-xs text-sm leading-6 text-slate-500">Choisissez la protection adaptée à vos trajets.</p><Link href="/boutique" onClick={close} className="mt-5 inline-flex rounded-full bg-slate-950 px-5 py-3 text-sm font-bold text-white">Découvrir la gamme</Link></div></div>}</aside></div>;
}

function Header() {
  const [open, setOpen] = useState(false); const [location] = useLocation(); const { itemCount, open: openCart } = useCart();
  return <><div className="hidden bg-slate-950 py-2.5 text-center text-[11px] font-medium tracking-wide text-slate-200 sm:block">Une dashcam choisie selon votre usage, pas selon une fiche technique.</div><header className="sticky top-0 z-50 border-b border-slate-200/80 bg-white/92 backdrop-blur-xl"><div className="container flex h-[68px] items-center justify-between gap-4"><BrandMark /><nav className="hidden items-center gap-6 lg:flex" aria-label="Navigation principale">{navItems.map((item) => <Link key={item.href} href={item.href} className={`text-sm font-semibold transition hover:text-[#1672d8] ${location === item.href ? "text-[#1672d8]" : "text-slate-600"}`}>{item.label}</Link>)}</nav><div className="flex items-center gap-2"><Link href="/quiz" className="hidden rounded-full bg-slate-950 px-4 py-2.5 text-xs font-bold text-white transition hover:bg-[#1672d8] sm:inline-flex">Trouver ma dashcam</Link><button onClick={openCart} className="relative grid h-10 w-10 place-items-center rounded-full bg-slate-100 text-slate-950 transition hover:bg-slate-200" aria-label="Ouvrir le panier"><ShoppingBag size={18} />{itemCount > 0 && <span className="absolute -right-1 -top-1 grid h-5 min-w-5 place-items-center rounded-full bg-[#1672d8] px-1 text-[10px] font-extrabold text-white">{itemCount}</span>}</button><button onClick={() => setOpen(!open)} className="grid h-10 w-10 place-items-center rounded-full bg-slate-950 text-white lg:hidden" aria-label="Ouvrir le menu">{open ? <X size={19} /> : <Menu size={20} />}</button></div></div>{open && <div className="border-t border-slate-100 bg-white px-4 py-4 lg:hidden"><nav className="mx-auto max-w-xl space-y-1">{navItems.map((item) => <Link key={item.href} href={item.href} onClick={() => setOpen(false)} className={`block rounded-xl px-4 py-3 text-sm font-bold ${location === item.href ? "bg-blue-50 text-[#1672d8]" : "text-slate-700 hover:bg-slate-50"}`}>{item.label}</Link>)}</nav></div>}</header></>;
}

function Footer() { return <footer className="bg-slate-950 pb-8 pt-12 text-slate-300 sm:pt-16"><div className="container"><div className="grid gap-10 border-b border-white/10 pb-10 sm:grid-cols-2 lg:grid-cols-[1.3fr_1fr_1fr_1fr]"><div><BrandMark dark /><p className="mt-5 max-w-xs text-sm leading-6 text-slate-400">La sécurité automobile, pensée avec précision pour conserver une preuve lorsque cela compte.</p></div><FooterColumn title="Découvrir" links={navItems} /><FooterColumn title="À savoir" links={[{ href: "/conseils", label: "Guide d’installation" }, { href: "/conseils", label: "Questions fréquentes" }, { href: "/conseils", label: "Livraison & retours" }]} /><div><h3 className="text-xs font-bold uppercase tracking-[0.15em] text-white">Rassurance</h3><p className="mt-4 text-sm leading-6 text-slate-400">Vos informations de paiement sont traitées lors du checkout sécurisé Shopify.</p></div></div><div className="flex flex-col gap-3 pt-6 text-xs text-slate-500 sm:flex-row sm:items-center sm:justify-between"><p>© {new Date().getFullYear()} NORTICAM. Tous droits réservés.</p><div className="flex gap-4"><a href="#legal" className="hover:text-white">Mentions légales</a><a href="#privacy" className="hover:text-white">Confidentialité</a></div></div></div></footer>; }
function FooterColumn({ title, links }: { title: string; links: { href: string; label: string }[] }) { return <div><h3 className="text-xs font-bold uppercase tracking-[0.15em] text-white">{title}</h3><div className="mt-4 grid gap-3 text-sm">{links.map((item) => <Link key={item.label} href={item.href} className="hover:text-white">{item.label}</Link>)}</div></div>; }

export default function StorefrontLayout({ children }: { children: ReactNode }) { return <div className="min-h-screen overflow-x-hidden bg-[#f7f8f9] text-slate-950"><Header /><main>{children}</main><Footer /><CartDrawer /></div>; }
