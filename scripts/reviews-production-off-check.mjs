import { chromium } from 'playwright';
import assert from 'node:assert/strict';
const browser=await chromium.launch({headless:true,channel:process.env.BROWSER_CHANNEL||'chrome'});const page=await browser.newPage();
try {await page.goto((process.env.TEST_URL||'http://127.0.0.1:4182')+'/produits/dashcam-3k-voiture/');await page.waitForSelector('[data-catalog-ready="true"]');assert.equal(await page.getByText('Aucun avis client publié sur cette fiche pour le moment.').count(),1);assert.equal(await page.getByText('Ils apparaîtront ici après publication.').count(),1);assert.equal(await page.locator('script[type="application/ld+json"]').evaluateAll(nodes=>nodes.some(node=>/AggregateRating|Review/.test(node.textContent||''))),false);console.log('Production review flag: off; no Review/AggregateRating schema: PASS');}finally{await browser.close();}
